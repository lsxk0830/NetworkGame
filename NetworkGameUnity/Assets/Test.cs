using System;
using UnityEngine;

public class Test : MonoBehaviour
{
    /*
    int[] array1 = new int[5] { 1, 2, 4, 5, 6 };
    int[] array2 = new int[10];
    void Start()
    {
        Array.Copy(array1, 2, array2, 3, 3);
        foreach (var item in array2)
            Debug.Log(item);
    }
    */

    /*
    private NetEvent mEvent = NetEvent.ConnectSucc;
    void Start()
    {
        NetManager.AddEventListener(mEvent,TestRemove);
        NetManager.RemoveEventListener(mEvent,TestRemove);
    }
    private void TestRemove(string str)
    {
    }
    */
}
